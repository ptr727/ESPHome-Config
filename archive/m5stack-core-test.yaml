substitutions:
  device_name: m5stack-core-test
  device_friendly_name: "M5Stack Test Device"

esphome:
  name: ${device_name}
  friendly_name: ${device_friendly_name}

esp32:
  board: m5stack-core-esp32
  framework:
    type: esp-idf

packages:
  common: !include templates/common.yaml
