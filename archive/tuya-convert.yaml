substitutions:
  device_name: tuya-convert
  device_friendly_name: "Tuya Convert"

# wifi:
  # use_address: tuya_convert

esphome:
  name: ${device_name}
  friendly_name: ${device_friendly_name}

esp8266:
  board: esp01_1m

packages:
  common: !include templates/common.yaml
