# Not for external use, it re-exports this repository's own root secrets.yaml.
# An external config keeps its secrets in a secrets.yaml beside itself, which templates resolve against.

---

  <<: !include ../secrets.yaml
