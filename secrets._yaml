# Replace substitutions and rename to secrets.yaml

wifi_ssid: "ssid"
wifi_password: "password"
wifi_domain: ".local"

# Generate with: openssl rand -base64 32
api_encryption_key: "REPLACE_WITH_BASE64_32_BYTE_KEY"
ota_password: "password"
